# Development override: use frontend-dev (Vite HMR) instead of production frontend
# Run: docker compose -f docker-compose.yml -f docker-compose.dev.yml up
# Open: http://localhost:5173 (hot reload enabled)

services:
  frontend:
    profiles:
      - production  # Only starts with: docker compose --profile production up
